# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Cross-compiled binaries from `agenthooks build`
dist/

# Settings-file backups written by `agenthooks install`
*.bak

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
*.prof

# Go workspace
go.work
go.work.sum

# Dependency directories
vendor/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Environment / secrets
.env
.env.*

# Internal-only docs (not part of the published package). The public docs linked
# from the README — WHATS-NEW, SCHEMA-REFERENCE, CONFLUENCE-PAGE, DEMO,
# VIDEO-STORYBOARD — are tracked.
docs/codebase/
docs/confluence-agent-hooks-framework.md